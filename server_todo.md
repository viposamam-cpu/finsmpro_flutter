# مطلوب إضافة واحدة بالسيرفر (وليست إعادة هيكلة)

تطبيق Flutter جاهز الآن للعمل بطريقتين:

1. **بدون أي تعديل بالسيرفر** — يعمل فوراً عبر fast-poll كل 3 ثوانٍ
   لكل سهم ظاهر بالشاشة فقط (`PriceStreamService.watchSymbols`).
   هذا أفضل بكثير من كاش الـ30 ثانية الحالي، لكنه ليس دفعاً حقيقياً.

2. **مع إضافة صغيرة وآمنة (سطر ~11327 عندك، بجانب `/api/brain_stream`
   الموجود فعلاً)** — أضف مسار SSE جديد `/api/price_stream` يبث أي
   tick جديد وصل من TradingView Webhook لحظة وصوله، بنفس نمط
   `brain_stream` الحالي تماماً (نفس الـ handler، فقط queue مختلف).
   هذا يحقق فعلياً "بدون تأخير 20 ثانية" لأن التطبيق لن ينتظر أي دورة
   استعلام، بل يستقبل السعر فور دخوله للسيرفر.

هذه الإضافة لا تلمس أي منطق تداول أو AI موجود — فقط بث موازٍ لما
يحدث أصلاً عند استلام الـ webhook. عندما تكون جاهز أطبّقها معك بنفس
قاعدة الـ `str_replace` الجراحي المتبعة بمشروعك.
