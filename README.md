# FinSMPro Omega — Flutter Client

## التشغيل (على جهازك، Flutter SDK غير متاح في بيئتي هنا)
```bash
flutter pub get
# عدّل kServerBaseUrl في lib/providers/app_providers.dart لرابط سيرفرك
flutter run
```

## ما تم بناؤه الآن (شغّال ومترابط، مو placeholder)
- **تخزين محلي كامل (SQLite)**: مراكز، صفقات مغلقة، كاش أسعار، قائمة
  مراقبة، سجل brain، عمليات معلّقة بانتظار المزامنة، إعدادات.
- **عميل API عام** يغطي كل الـ 40+ مسار بالسيرفر عبر `get()/post()`،
  بالإضافة لدوال مختصرة لأهم المسارات (portfolio, positions, buy, sell...).
- **بث أسعار حي**: SSE أولاً (بعد إضافة `/api/price_stream`، راجع
  `server_todo.md`)، وfast-poll كل 3 ثوانٍ كخطة احتياطية — بدل تأخير
  الكاش الحالي 30 ثانية.
- **آلية أوفلاين**: أي عملية (شراء/بيع) تفشل بسبب انقطاع النت تُخزَّن
  بجدول `pending_ops` وتُعاد تلقائياً عند عودة الاتصال.
- **شاشة المحفظة**: تفتح فوراً من البيانات المحلية (لا شاشة فارغة)،
  ثم تتحدث لحظياً وتُظهر مصدر السعر وعمره بالثواني لكل سهم.

## ✅ تم إضافته لاحقاً
1-10. (راجع التاريخ أعلاه: تحليل فني، مراقبة، مخاطر، عقل الآلي، شات،
   إعدادات، مزامنة أوفلاين، تشخيص اتصال منفصل سيرفر/قاعدة بيانات).
11. **تنفيذ شراء/بيع فعلي من التطبيق** — `_TradeActionBar` بشاشة
    التحليل: زر شراء (بحوار لإدخال عدد الأسهم) وزر بيع (يظهر فقط لو
    يوجد مركز مفتوح لهذا السهم فعلاً، بعدد الأسهم المملوكة).
    **صححت خطأ اكتشفته أثناء البناء**: `/api/buy` و`/api/sell`
    بالسيرفر هما GET بمعاملات query (`sym, name, shares, price...`)
    وليس POST كما كتبتهما بعميل الـ API أول مرة — صححت التوقيعين
    بالكامل ليطابقا السيرفر تمامًا.
12. **سجل الصفقات المغلقة** (`ClosedTradesScreen`) — ملخص صافي الربح/
    الخسارة الكلي وعدد الصفقات الرابحة/الخاسرة، وقائمة تفصيلية لكل
    صفقة. **صححت أيضاً**: حقول جدول `closed_trades` الفعلية هي
    `buy_price/sell_price/net_pnl/commission/duration` — ليست الحقول
    الافتراضية (`entry/exit/pnl`) التي افترضتها بمرحلة سابقة قبل
    فحص كود الإدراج الفعلي بالسيرفر. الوصول لها من أيقونة 🕐 بشاشة المخاطر.
13. **الأخبار لكل سهم** (`_NewsSection` بشاشة التحليل) — تُجلب مباشرة
    من `/api/news` (الحقول الفعلية: title/date/source/url)، بدون كاش
    (الأخبار تتغيّر باستمرار).

## قاعدة البيانات
`PG_URL` بـ `finsmpro_v55_server_PATCHED.py` **مطابقة تمامًا** للملف
الأصلي `finsmpro_v55_server.py` — لم تُعدَّل بناءً على طلب صريح.

## خارطة الطريق التالية
1. اختبار على جهاز حقيقي (`flutter run`) وإصلاح أي خطأ compilation فعلي
2. توليد APK للنشر (`flutter build apk`)
3. (اختياري) شموع Candlestick تفاعلية حقيقية بدل الخط الحالي
4. (اختياري) شاشة مخصصة لإدارة قائمة المراقبة يدويًا (إضافة/حذف أسهم)
